#include <stdio.h>
#include <stdlib.h>
/**
 * main-program that prints all single digit numbers of base 10 starting from 0
 * Return: Always 0 (succes)
 */
int main(void)
{
int n;
for (n = 0; n < 10; n++)
{
printf("%d", n);
}
return (0);
}
