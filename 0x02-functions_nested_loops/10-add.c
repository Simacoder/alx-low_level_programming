#include "main.h"
/**
 * add - function that adds two integers and returns the result.
 * @x:parameter member
 * @y:parameter member
 * Return: x+ y always 0success
 */
int add(int x, int y)
{
	return (x + y);
}
