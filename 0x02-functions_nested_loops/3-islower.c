#include "main.h"
/**
 * _islower -function that checks for lowercase character
 * @c: parameter member
 * Return: returns 1 or 0 if success
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
