#include "main.h"

/**
 * _strlen - function that returns the length of a string
 * @s: paramter member
 * Return: return 0 sucess
 */

int _strlen(char *s)
{
	int count;

	for (count = 0; s[count] != '\0'; s++
	return (count);
}

